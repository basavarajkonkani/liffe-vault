# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
# Generate a secure random string (min 256 bits / 32 characters)
JWT_SECRET=your-secret-key-here-min-256-bits-change-this-in-production

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
# Use the service_role key (NOT the anon key) for backend
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Frontend URL for CORS
# Update this to your frontend domain in production
FRONTEND_URL=http://localhost:5173
